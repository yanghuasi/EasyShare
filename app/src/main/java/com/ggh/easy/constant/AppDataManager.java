package com.ggh.easy.constant;

import com.orhanobut.hawk.Hawk;


/**
 * App数据存取操作
 */
public class AppDataManager {
/*

    private User mUser;

    private boolean isLogin = false;

    private static AppDataManager instance;

    public static synchronized AppDataManager getInstance() {
        if (instance == null) {
            synchronized (AppDataManager.class) {
                if (instance == null)
                    instance = new AppDataManager();
            }
        }

        return instance;
    }

    public static <T> boolean put(String key, T value) {
        return Hawk.put(key, value);
    }

    public static <T> T get(String key, T defaultValue) {
        return Hawk.get(key, defaultValue);
    }

    public static <T> T get(String key) {
        return Hawk.get(key);
    }

    public boolean isLogin() {
        isLogin = get(Constants.Login, isLogin);
        return isLogin;
    }

    public void setLogin(boolean login) {
        isLogin = login;
        put(Constants.Login, isLogin);
    }

    public void setUser(User user) {
        mUser = user;
        put(Constants.User, user);
    }

    public User getUser() {
        if (mUser == null)
            mUser = get(Constants.User);
        return mUser;
    }
*/


}
